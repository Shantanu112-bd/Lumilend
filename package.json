{
    "name": "lumilend-root",
    "version": "1.0.0",
    "description": "LumiLend Root Workspace",
    "private": true,
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "build": "npm run build --prefix frontend",
        "test:contract": "cargo test --manifest-path contracts/lumilend/Cargo.toml -- --nocapture"
    }
}